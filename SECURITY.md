# 安全政策

## 项目状态

dieΠ 是 Alpha 项目，`0.1.0` 已公开发布。正式的长期支持周期尚未建立；安全修复以最新
`0.1.x` 补丁为目标，旧补丁可能不回移植，修复也可能收紧此前接受的输入或路径行为。

框架会执行受信任的本地 Python 策略代码。它不是用来运行不受信任策略、插件、笔记本
或下载代码的安全沙箱。行情和结果目录也是本地研究输入，不构成安全边界。

GUI 与 CLI 具有相同的本地信任模型。安装自 wheel 或使用 `diepi gui` 不会把策略执行、
文件访问或第三方依赖放进额外沙箱。dataset manifest 和 RunArtifact hash 用于完整性验证，
不是恶意内容扫描、代码签名或数据授权证明。

## 报告安全问题

请不要在公开 issue、discussion、pull request、示例或文档中披露疑似漏洞。

请使用 GitHub 的
[私密漏洞报告入口](https://github.com/elonmaskhair-prog/diepi/security/advisories/new)。该入口
需要登录 GitHub；不要用公开 issue 临时代替。如果入口不可用，请先停止披露，并通过
[维护者 GitHub 主页](https://github.com/elonmaskhair-prog)请求建立私密联系，不要在联系请求中
附带利用细节、凭据、行情或交易记录。

报告应尽量包括：

- 受影响的修订和运行环境；
- 不含凭据和私有行情的最小复现；
- 预期行为、实际影响；
- 利用是否需要不受信任的策略代码、特制归档、文件系统或网络访问；
- 已知缓解措施或建议修复。

维护者会通过同一私密渠道确认报告、评估影响，并在修复和回归测试就绪后协调披露。只有
公共安全流程实际建立后，项目才会公布响应时限或保密期承诺。

## 敏感材料

不要附带凭据、私钥、账户记录、专有策略、个人绝对路径、受许可约束的原始行情或真实
交易记录。请使用合成夹具和可移植占位符替代。

`diepi demo` 生成的数据可以用于无敏感信息复现；报告时请保留其 synthetic 标识，不要
用真实行情覆盖 demo 工作区后再把它作为公开附件。
